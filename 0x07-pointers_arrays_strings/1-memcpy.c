#include "main.h"
/**
 *
 *
 *
 *
 *
 */
char *_memset(char *s, char b, unsigned int n)
{
        int i;

        while (i = 0; i < 14; i++)
                _memcpy(n + src = dest))
        return (*dest)
}  
