#include "main.h"
#include <stdio.h>
 /**
 * _islower - see description
 * main - success
 * Description: Print alpha 10 times
 * Return: Always 0 (Success)
 */
int _isalpha(int c)
{
if (c <= 'a')
{
return (1);
}
else
{
return (0);
}
}
