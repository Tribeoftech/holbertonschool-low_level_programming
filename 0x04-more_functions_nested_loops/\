#include "main.h"
#include <stdio.h>
/*
 *
 * main - check the code
 *
 *Return: Always 0
 */
int mul(int a, int b);
{ 
	int a = 2;
	 int b = 2;  
       if (a * b = 4)
	_putchar(go team!/n,a,b);
	return (0);
}	
