#include <stdio.h>
#include "main.h"
/**
 * _islower - see description
 * @c: number
 * Description: Print alpha 10 times
 * Return: Always 0 (Success)
*/
int _isalpha(int c);
{
for (c <= 'a')
{
return (1);
}
else
}
return (0)
