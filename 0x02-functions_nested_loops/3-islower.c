#include "main.h"
#include <stdio.h>
 /**
 * _islower - returns True if all the characters are in lower case, or false
 * main - success
 *
 * Return: Always 0 (Success)
 */
int _islower(int c)
{
if (c <= 'a')
{
return (1);
}
else
{
return (0);
}
}
